import React, { Component } from 'react';
import {
  StyleSheet,
  View,
  Text,
  Button
} from 'react-native';

export default class AddMirror extends Component {

    render(){
        return (
            <>
            <Text>Add a mirror</Text>
            </>
        )
    }
}

const styles = StyleSheet.create({
});
